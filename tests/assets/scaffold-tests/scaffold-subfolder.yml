inheritsFrom:
  - ./questions.yml

assets:
  - ./scaffold-utils.yml

utilsModule:
  - utilsModule/%shortName%_utils/%shortName%_utils.info.yml
  - utilsModule/%shortName%_utils/%shortName%_utils.install
  - utilsModule/%shortName%_utils/%shortName%_utils.module

scaffold:
  - "echo 'Project: %name%'"
  - "echo 'Shortname: %shortName%'"
  - copy_assets(%rootFolder%/web/modules/custom, utilsModule)