requires: 3.0

questions: []
assets:
    - test-twig.json

variables:
  slugInput: "A test string to be slugified"
  md5Input: " a string that will be hashed by md5"

scaffold:
  - mkdir -p %rootFolder%
  - set_directory(%rootFolder%)
  - copy_assets(%rootFolder%, assets)
