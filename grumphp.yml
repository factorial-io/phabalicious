parameters:
    process_timeout: 180
    stop_on_failure: true
    git_dir: .
    bin_dir: vendor/bin
    tasks:
      composer: ~
      phplint: ~
      phpcs:
        standard:
          - PSR2
      phpunit:
        group: ['default']
      phpstan: ~

