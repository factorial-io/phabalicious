parameters:
    process_timeout: 180
    git_dir: .
    bin_dir: vendor/bin
    tasks:
      composer: ~
      phplint: ~
      phpcs:
        standard:
          - PSR2
      phpunit:
        group: ['default']
      phpstan: ~

